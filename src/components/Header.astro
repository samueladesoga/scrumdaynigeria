---
interface NavItem {
  label: string;
  href: string;
}

interface Props {
  nav: NavItem[];
  ticketUrl: string;
}

const { nav, ticketUrl } = Astro.props;
---

<header class="site-header" id="top">
  <div class="container header-wrap">
    <div class="brand-group">
      <a href="/" class="brand-mark" aria-label="Scrum Day Nigeria home">
        <img class="brand-logo" src="/brand-icon.png" alt="Scrum Day Nigeria" />
      </a>
      <span class="organizer-credit">
        <span class="organizer-divider" aria-hidden="true"></span>
        Organized by
        <a href="https://www.valuehut.co/" target="_blank" rel="noopener noreferrer" aria-label="ValueHut Consulting website">
          <img class="organizer-logo" src="/valuehut-logo.svg" alt="ValueHut Consulting Limited" />
        </a>
      </span>
    </div>

    <nav aria-label="Main navigation" class="main-nav">
      <ul class="nav-list">
        {nav.map((item) => (
          <li>
            <a href={item.href}>{item.label}</a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="header-cta">
      <a href={ticketUrl} class="btn-primary">Tickets</a>
    </div>
  </div>
</header>

<style>
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: #ffffff;
  border-bottom: 1px solid #ececec;
  width: 100%;
  pointer-events: auto;
}

.header-wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 1.2rem;
  width: 100%;
  max-width: unset;
  min-height: 76px;
}

.brand-group {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  flex: none;
}

.brand-mark {
  width: 38px;
  height: 38px;
  border-radius: 999px;
  background: rgb(255 255 255 / 92%);
  border: 1px solid rgba(255, 255, 255, 0.5);
  display: grid;
  place-items: center;
  text-decoration: none;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
  flex: none;
}

.organizer-credit {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  font-size: 0.78rem;
  color: #6b7275;
  white-space: nowrap;
}

.organizer-logo {
  height: 20px;
  width: auto;
  display: block;
}

.organizer-divider {
  width: 1px;
  height: 22px;
  background: #e2e2e2;
  flex: none;
}

.brand-logo {
  width: 34px;
  height: 34px;
  object-fit: contain;
  display: block;
}

.main-nav {
  flex: 1;
  margin: 0 1rem;
}

.nav-list {
  display: flex;
  list-style: none;
  margin: 0;
  padding: 0;
  gap: 0.35rem;
  align-items: center;
}

.nav-list li a {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.58rem 1rem;
  border-radius: 999px;
  background: #ffffff;
  color: #202225;
  text-decoration: none;
  font-weight: 700;
  font-size: 0.9rem;
  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.5), 0 8px 18px rgba(0, 0, 0, 0.08);
  transition: all 0.2s ease;
  border: 1px solid transparent;
}

.nav-list li a:hover {
  background: #f6f1e8;
  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.5), 0 8px 18px rgba(0, 0, 0, 0.12);
}

.header-cta {
  display: flex;
  gap: 0.8rem;
  align-items: center;
}

.btn-primary {
  padding: 0.7rem 1.4rem;
  background: var(--brand-primary);
  color: white;
  text-decoration: none;
  border-radius: 999px;
  font-weight: 700;
  font-size: 0.9rem;
  box-shadow: 0 4px 12px rgba(255, 79, 18, 0.3);
  transition: all 0.2s ease;
  border: none;
  cursor: pointer;
}

.btn-primary:hover {
  background: var(--brand-primary-dark);
  box-shadow: 0 6px 16px rgba(255, 79, 18, 0.4);
  transform: translateY(-1px);
}

@media (max-width: 768px) {
  .header-wrap {
    gap: 1rem;
  }

  .organizer-credit {
    display: none;
  }

  .main-nav {
    margin: 0 1rem;
    flex: 1;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .main-nav::-webkit-scrollbar {
    display: none;
  }

  .nav-list {
    gap: 0.3rem;
  }

  .nav-list li a {
    padding: 0.5rem 0.75rem;
    font-size: 0.8rem;
    white-space: nowrap;
  }

  .btn-primary {
    padding: 0.6rem 1rem;
    font-size: 0.85rem;
  }
}
</style>
